OK, I need a single webpage app that can be used for training events for people to understand how to use an agentic agent to perform simple tasks. This agent will run entirely inside the uses web browser. It will include a web app of Python and related software like pandas so that the user can upload documents such as Excel documents, Microsoft Word documents, and then ask the agent to perform actions on them. Also the agentic should be able to web searches and process the results or  download documents and data into the Web application. The backend - an LLM-  will run using an remote API key on Lumen.ncsa.illinois.edu so that API endpotin and keuy will need to be entered, but does not need to be part of the Web App app (but it could be included on the address line for ease of use at training event) . Lumen supports an openAI API endpoint (+model). The results of the API call will need to be parsed for tool use; I  thinkwe only few toolse.g.  able to run Python in the browser, list files in the virtual file system, and be able to understand the local files that we've downloaded or uploaded into this area and perform and basic actions on those files. I will be serving this app either git hub pages or or just as a a simple static web page.